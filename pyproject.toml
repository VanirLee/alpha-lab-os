[project]
name = "alpha-lab-os"
version = "0.1.0"
description = "Point-in-time on-chain quantitative research lab using Binance Web3 API"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
alpha-lab = "alpha_lab_os.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]

